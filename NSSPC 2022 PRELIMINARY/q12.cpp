#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n, x;
    int a;
    cin >> n >> x;
    int temp=n;
    vector<double> v;

    while (temp--) {
        cin >> a;
        v.push_back(a);
    }

    
}